如何解析SQL语句：
	老师布置的题目中只要求了处理Select语句。
	为此我们要做的是，词法分析，语法分析。数据的存储和读取。
	首先说词法语法分析，这个没什么特别要注意的，就是早点做出来多多进行测试，把功能做得完善一些。
然后就是数据的存储了。数据库的内容是表的形式呈现出来的，而现在我能够想到的，我估计大家也都会采用
的数据存储方式，就是XML了。
	确定一下数据的对应关系。
	database -> xml文件（这层也许不用做，因为select语句是在数据库确定时执行的）
	table -> <table>标签
	key-value  -> <key> <value>  标签